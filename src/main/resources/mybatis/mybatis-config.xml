<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 暂时不需做任何配置 -->

	<!-- 使用的是注解 -->
	<mappers>
		<!-- <mapper class="com.htao.dao.IAccountDao"/> --> <!-- 该包下所有的dao接口都可以使用 -->
		<package name="com.htao.programmer.dao"/>
	</mappers>

</configuration>